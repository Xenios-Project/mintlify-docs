---
title: "Introduction"
description: "Place and manage orders, stream live market and account data, and read wallets, deposits and reference data — all behind one authenticated gateway."
---

The Xenios Trading API lets you place and manage orders, stream live market
and account data, and read wallets, deposits and reference data — all behind a
single, authenticated front door.

Everything is served from one gateway. REST and WebSocket traffic share the
same host and port; you never talk to internal services directly.

## Base URL

All REST endpoints live under a single prefix:

```
https://<your-host>/api/v1/trading
```

For local development that is typically:

```
http://localhost:5678/api/v1/trading
```

TLS is terminated at the edge — use `https://` (and `wss://` for sockets) in
any deployed environment.

## What you get

- **REST API** — place/cancel orders, list and search your orders, read
  wallets, balances, deposits, products, assets and the order book.
- **WebSocket streams** — real-time order book, order, wallet and deposit
  updates over a single authenticated socket. See [WebSockets](/guides/websockets).
- **HMAC authentication** — every request is signed; nothing relies on a
  long-lived bearer token. See [Authentication](/guides/authentication).

## How a request works

1. You receive an **API key id** and a **secret** during onboarding.
2. For each request you build a *canonical string* (method, path, timestamp,
   nonce, content-type, body hash) and sign it with your secret using
   HMAC-SHA256.
3. You send the signature plus a few headers. The gateway re-computes the
   signature, checks the timestamp and nonce, verifies your key's permissions,
   and forwards the call.

The signing recipe is identical for REST and WebSocket handshakes — learn it
once in [Authentication](/guides/authentication) and reuse it everywhere.

## Quickstart

<Steps>
  <Step title="Get credentials">
    Obtain your `keyId` (looks like `api_xenios_…`) and secret from your account
    contact. Store the secret in a secret manager — never commit it.
  </Step>
  <Step title="Sign and call">
    Build the auth headers (see [Authentication](/guides/authentication)) and
    call a read endpoint, e.g. `GET /api/v1/trading/order/open`.
  </Step>
  <Step title="Place an order">
    `POST /api/v1/trading/order` with a limit order body. See [Orders](/guides/orders).
  </Step>
  <Step title="Stream updates">
    Open the WebSocket at `/api/v1/trading/ws` and subscribe to the channels you
    care about. See [WebSockets](/guides/websockets).
  </Step>
</Steps>

## Conventions

- Requests and responses are **JSON**.
- Decimal amounts (quantities, prices, balances) are sent as **strings** to
  avoid floating-point precision loss.
- Timestamps are **ISO-8601** strings unless noted otherwise.
- Errors use standard HTTP status codes — see
  [Errors & Rate Limits](/guides/errors).

Browse the full endpoint catalogue in the **API Reference**.
